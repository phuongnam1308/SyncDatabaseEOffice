/**
 * ============================================================
 * roleMapping.js - Department Role Mapping
 * ============================================================
 * Stub for department sync (departments don't have role-based mapping).
 */

const departmentMapping = [];

module.exports = { departmentMapping };
